"$schema" = 'https://starship.rs/config-schema.json'
palette = "noctalia"

# Line 1: path + git | exec time right-aligned
# Line 2: ❯
format = """
$username$hostname$directory$git_branch$git_status\
$fill\
$cmd_duration
$character"""

add_newline = false

# Full path in Kanagawa blue — zen 'style = full'
[directory]
format = "[$path]($style)"
style = "blue bold"
truncation_length = 0
truncate_to_repo = false
read_only = " "

# ' branch' in grey — nerd font icon matches zen branch_icon=''
[git_branch]
format = "[ $branch]($style)"
style = "subtext0"
symbol = " "

# * dirty (working or staging), ⇣⇡ in teal/cyan — zen cyan for ahead/behind
[git_status]
format = '([$all_status](subtext0)[$ahead_behind](cyan) )'
modified = "*"
staged = "*"
untracked = ""
deleted = ""
renamed = ""
conflicted = ""
stashed = ""
ahead = "⇡"
behind = "⇣"
diverged = "⇡⇣"
up_to_date = ""

[fill]
symbol = " "

# Exec time — threshold 5000ms, yellow — exact match to zen
[cmd_duration]
min_time = 5000
format = "[$duration]($style)"
style = "yellow"

# ❯ — magenta success, red error — exact match to zen
[character]
success_symbol = "[❯](magenta)"
error_symbol = "[❯](red)"
vimcmd_symbol = "[❮](magenta)"

# Minimal — zen shows nothing else
[username]
show_always = true
format = "[$user](green)[@](white)"
style_user = "green"
style_root = "red bold"
disabled = false

[os]
disabled = true

[hostname]
ssh_only = false
format = "[$hostname ]($style)"
style = "#ffa066 bold"
trim_at = ""
disabled = false
[time]
disabled = true
[battery]
disabled = true
[package]
disabled = true
[nodejs]
disabled = true
[python]
disabled = true
[rust]
disabled = true
[golang]
disabled = true
[java]
disabled = true
[lua]
disabled = true
[docker_context]
disabled = true

# >>> NOCTALIA STARSHIP PALETTE >>>
# Noctalia Starship Palette
# Generated by Noctalia - do not edit manually

[palettes.noctalia]
# Standard colors
blue      = "#b94de6"
red       = "#fd4663"
green     = "#6289e9"
yellow    = "#8166ff"
cyan      = "#a38fef"
magenta   = "#8fabef"
white     = "#f2f2f3"
black     = "#0a1c48"

# Extended palette (Catppuccin-compatible names)
rosewater = "#8166ff"
flamingo  = "#fd4663"
pink      = "#8fabef"
mauve     = "#8fabef"
maroon    = "#fd4663"
peach     = "#8166ff"
teal      = "#a38fef"
sky       = "#a38fef"
sapphire  = "#b94de6"
lavender  = "#8fabef"

# Text shades
text      = "#f2f2f3"
subtext1  = "#f2f2f3"
subtext0  = "#5f646f"

# Surface shades
overlay2  = "#5f646f"
overlay1  = "#5f646f"
overlay0  = "#0a1c48"
surface2  = "#0a1c48"
surface1  = "#0a1c48"
surface0  = "#071536"
base      = "#071536"
mantle    = "#071536"
crust     = "#071536"
# <<< NOCTALIA STARSHIP PALETTE <<<
